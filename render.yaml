services:
  - type: web
    name: n8n
    runtime: docker
    plan: standard
    disk:
      name: n8n-data
      mountPath: /home/node/.n8n
      sizeGB: 1
    envVars:
      - key: PORT
        value: 5678
      - key: WEBHOOK_URL
        sync: false